module NakedObjects.Gemini.Test.Helpers {
    import PaneRouteData = Angular.Gemini.PaneRouteData;
    import INakedObjectsScope = Angular.INakedObjectsScope;

    export enum SetupCondition {
        Home
    }

    export function setup(condition: SetupCondition, paneRouteData: PaneRouteData) {

    }


    export function verify(testScope: INakedObjectsScope) {

    }
}