using NakedFunctions;
using System.Linq;

namespace SchoolRecords.Model
{
    public static  class TeachingSet_MenuFunctions
    {
        //TODO:

    //    public static TeachingSet CreateNewSet(IContext context)
    //    {
    //        return Container.NewTransientInstance<TeachingSet>();
    //    }

    //    public static IQueryable<TeachingSet> ListSets([Optionally] Subject subject, [Optionally] int? yearGroup)
    //    {
    //        var sets = Container.Instances<TeachingSet>();
    //        if (subject != null)
    //        {
    //            int id = subject.Id;
    //            sets = sets.Where(s => s.Subject.Id == id);
    //        }
    //        if (yearGroup != null)
    //        {
    //            sets = sets.Where(s => s.YearGroup == yearGroup.Value);
    //        }
    //        return sets.OrderBy(s => s.YearGroup).ThenBy(s => s.Subject.Name);
    //    }
    }
}
